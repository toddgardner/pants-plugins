from setuptools import setup

setup(name='verst',
      version='0.1',
      description='Verst pants plugins',
      url='http://actioniq.com',
      author='Mitesh Patel',
      author_email='mitesh@actioniq.com',
      license='MIT',
      packages=['verst'],
      zip_safe=False)
